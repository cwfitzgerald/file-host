<body>
    <table>
        <thead>
            <th>Filename</th>
            <th>Size</th>
        </thead>
        {{#each files}}
            <tr>
                <td><a href="{{this.[1]}}">{{this.[0]}}</a></td>
                <td>{{this.[2]}}</td>
                <td><a href="{{this.[3]}}">delete</a></td>
            </tr>
        {{/each}}
    </table>
</body>
