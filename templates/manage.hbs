<style>
    th {
        padding-left: 10px;
        padding-right: 10px;
        text-align: center;
    }
    td {
        padding-left: 10px;
        padding-right: 10px;
        text-align: center;
    }
</style>

<body>
    <table>
        <thead>
            <th>File</th>
            <th><a href="{{sort_date}}">Date</a></th>
            <th><a href="{{sort_size}}">Size</a></th>
            <th>Removal</th>
        </thead>
        {{#each files}}
            <tr>
                <td><a href="{{this.[1]}}">{{this.[0]}}</a></td>
                <td>{{this.[2]}}</td>
                <td>{{this.[3]}}</td>
                <td><a href="{{this.[4]}}">delete</a></td>
            </tr>
        {{/each}}
    </table>
    Total: {{total}}
    <br>
    Size: {{total_size}}
</body>
